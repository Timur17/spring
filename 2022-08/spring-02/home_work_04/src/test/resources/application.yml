spring:
  messages:
    basename: "i18n/appmessages"
    encoding: "UTF-8"
  shell:
    interactive:
      enabled: false

management:
  endpoints:
    web:
      exposure:
        include: info, health, beans

server:
  port: 8080

application:
  locale: ru_RU


